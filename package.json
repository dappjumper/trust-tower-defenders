{
  "name": "trust-tower-defenders",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "setup": "npm install && cd app && npm install && cd ..",
    "build": "cd app && npm run build && cd ..",
    "develop": "npm-run-all --parallel start develop-app",
    "develop-app": "cd app && node build/dev-server.js",
    "serve": "npm run build && npm run start",
    "start": "npm run setup && npm run serve",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": "12.16.x"
  },
  "author": "DappJump.io",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "express-jwt": "^5.3.1",
    "pixi.js": "^5.2.1",
    "socket.io": "^2.3.0",
    "vue": "^2.6.11"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
